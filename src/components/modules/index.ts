import type { ModuleInterface, ModuleRegistry } from "@/types/module";

export {
  moduleRegistry,
  enabledModules,
  hearingModule,
  speechModule,
  visualModule,
  mobilityModule,
} from "@/lib/module-registry";

export type { ModuleInterface, ModuleRegistry };
